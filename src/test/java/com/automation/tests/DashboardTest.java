package com.automation.tests;

/* import com.automation.base.BaseTest;
	import com.automation.pages.DashboardPage;
	import org.testng.Assert;
	import org.testng.annotations.Test;
	import java.nio.file.Paths;

	public class DashboardTest extends BaseTest {

	    @Test(enabled = false)
	    public void testDownloadOptions() throws InterruptedException {

	      
	        DashboardPage dashboard = new DashboardPage(driver);

	        dashboard.downloadSVG();
	        Thread.sleep(3000); // Wait for file to download
	        String svgPath = Paths.get(System.getProperty("C:\\Users\\Admin"), "Downloads", "filename.svg").toString();
	        Assert.assertTrue(dashboard.isDownloadSuccessful(svgPath), "SVG download failed!");

	        dashboard.downloadPNG();
	        Thread.sleep(3000);
	        String pngPath = Paths.get(System.getProperty("C:\\Users\\Admin"), "Downloads", "filename.png").toString();
	        Assert.assertTrue(dashboard.isDownloadSuccessful(pngPath), "PNG download failed!");

	        dashboard.downloadCSV();
	        Thread.sleep(3000);
	        String csvPath = Paths.get(System.getProperty("C:\\Users\\Admin"), "Downloads", "filename.csv").toString();
	        Assert.assertTrue(dashboard.isDownloadSuccessful(csvPath), "CSV download failed!");
	    }
	}

	}*/
	


package com.automation.pages;

 /*import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;

	public class DashboardPage {
	    private WebDriver driver;

	    // Locators
	    private By menu = By.cssSelector("div[id='apexchartsjoitw872'] div[title='Menu']"); 
	    private By svgDownload = By.tagName("div");
	    private By pngDownload = By.className("apexcharts-menu-item exportPNG");
	    private By csvDownload = By.className("apexcharts-menu-item exportCSV");

	    // Constructor
	    public DashboardPage(WebDriver driver) {
	        this.driver = driver;
	    }

	    // Methods
	    public void Menu() {
	        driver.findElement(menu).click();
	    }

	    
	    public void downloadSVG() {
	        driver.findElement(svgDownload).click();
	    }

	    public void downloadPNG() {
	        driver.findElement(pngDownload).click();
	    }

	    public void downloadCSV() {
	        driver.findElement(csvDownload).click();
	    }

	    public boolean isDownloadSuccessful(String filePath) {
	        return new java.io.File(filePath).exists();
	    }
	}
	*/
	
	
	
	
